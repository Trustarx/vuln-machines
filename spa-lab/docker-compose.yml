services:
  frontend:
    image: nginx:alpine
    container_name: spa-lab-frontend
    ports:
      - "8093:80"
    volumes:
      - ./frontend:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
    restart: unless-stopped

  backend:
    build: ./backend
    container_name: spa-lab-backend
    restart: unless-stopped
    # Internal only — not exposed directly. Reached via nginx proxy /api/*
